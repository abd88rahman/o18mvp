# Status Ringkasan Requirement - app/mvp

Catatan pelacakan status tiap dokumen requirement di folder ini, supaya gampang lanjut sesi berikutnya tanpa perlu baca ulang semua file. Update manual tiap kali ada progres/keputusan baru.

**RENAME (2026-08-27)**: modul yang di dokumen ini masih banyak disebut `c18_account` sudah di-rename teknis jadi **`c18_basic_erp`** (folder `app/mvp/c18_basic_erp/`) — nama lama dianggap menyesatkan karena isinya sudah jauh lebih dari sekadar GL/Accounting (ada Purchase, Sales, Inventory costing, Payroll, Fixed Assets — sesuai keputusan tiering "Basic cukup 1 modul"). Nama **model** Odoo (`c18.account.*`, `c18.purchase.*`, dst) TIDAK berubah, cuma technical module name-nya. Referensi path/nama modul di teks di bawah yang belum diupdate mengikuti histori penulisan aslinya.

Terakhir diupdate: 2026-08-30 (Skenario testing PT Roda Sejahtera - `testing/mvp/` - dijalankan penuh via script `odoo shell` ke database `test-roda`, 17 bulan transaksi Nov 2024-Apr 2026 + batch gap-coverage Maret + 3 edge case, SEMUA PASS & di-commit. Validasi backend end-to-end, bukan UI manual. 1 bug arithmetic ditemukan & diperbaiki di dokumen skenario (amortisasi sewa 120jt/tahun butuh 12 bulan bukan 10, checkpoint dipindah dari 31 Agu ke 31 Okt 2025), 1 bug produk ditemukan & diperbaiki di kode (`res_company_views.xml` referensi menu yang belum di-load, crash total di instalasi database baru manapun - lihat detail di poin `c18_basic_erp` bawah). Update sebelumnya: 2026-08-30 (Sistem Pencatatan Persediaan Perpetual/Periodik - `inventory_system` di Settings company, DEFAULT Perpetual - SELESAI diimplementasikan & diverifikasi via odoo shell: mode Periodik = HPP dihitung 1x di Stok Opname pakai formula Persediaan Awal+Pembelian-Persediaan Akhir, bukan per-transaksi; akun baru "Pembelian" 5-1100; lihat detail [06-accounting-business.md](06-accounting-business.md) poin F.3. Juga `costing_method` default diubah FIFO (sebelumnya Average) - cuma berlaku company/database baru, tidak retroaktif). Update sebelumnya: 2026-08-30 (7 Laporan Keuangan + Kartu Stok/Saldo Persediaan SELESAI diimplementasikan & diverifikasi — arsitektur AbstractModel+controller+OWL baru dipakai; ditemukan & diperbaiki gap mesin costing: FIFO tidak nyimpan histori per-layer, Average tidak nyimpan histori sama sekali, 2 model audit log baru ditambah). Update sebelumnya: 2026-08-30 (demo data untuk presentasi client selesai & terverifikasi — lihat [08-demo-data.md](08-demo-data.md), termasuk keterbatasan Odoo ditemukan: mekanisme 'demo' bawaan tidak bisa dipakai untuk modul `auto_install`). Update sebelumnya: 2026-08-30 (gap multi-currency di agregasi Tutup Buku/rekonsiliasi Aktiva Tetap ditemukan & diperbaiki + diverifikasi, lihat detail di poin `c18_basic_erp` bawah), 2026-08-27 (`c18_account` mulai discaffold di `app/mvp/c18_basic_erp/` — model fondasi GL (CoA/Journal/Move/Move Line/Cost Center/Exchange Rate) + Partner extend + Product tier Basic sudah ada, plus data default CoA (41 akun) & 30 master Journal. **Belum smoke test.** Form transaksi (Kas Bank, PO ringan, generic Pembelian/Penjualan, Aktiva Tetap, Payroll, costing FIFO/Average) masih fase berikutnya. Juga dikoreksi: `c18_common` sbg modul terpisah TIDAK dipakai lagi — Partner/Product Basic jadi bagian `c18_account` sesuai [00-tiering-produk.md](../00-tiering-produk.md).) (07 direview sebagian — modul digabung jadi `c18_hris` (bukan `c18_hr`/`c18_hr_payroll` terpisah), riwayat kontrak bertanggal dikonfirmasi, Attendance/Time Off masuk MVP versi sederhana (HRIS tier Enterprise tapi tetap MVP inti, bukan full-custom), Cost Center & salary rule fleksibel & UMK BPJS dikonfirmasi. Sisa: riset 3 repo referensi payroll blm dilakukan.).

## Sudah Selesai / Cukup

| Dokumen | Ringkasan |
|---|---|
| **Fondasi Accounting/GL** ([01](01-accounting-foundation.md)) | **Cuma fondasi** (level sama dg `c18_common`), bukan modul "Accounting" MVP itu sendiri — lihat [06](06-accounting-business.md) utk itu. Chart of Accounts (data default + 15 tipe akun ala Accurate), ~25 tipe Journal, Account Move/Move Line (`c18.account.*`) + generic reference, Cost Center (field di move line), Multi-currency (kurs transaksi + tabel kurs harian), Periode akuntansi (deteksi murni dari tanggal, tanpa field tambahan). |
| **Fondasi Master Data Bersama** ([02](02-common-master-data.md)) | Partner (`_inherit res.partner`, **semua tier** — multi-alamat/hierarki/VAT sudah gratis dari bawaan), Product (`c18.product`, **Standard+**: tipe Barang Stok/Jasa/Non Stok, bisa dibeli dan/atau dijual), UoM (`c18.uom.uom`, **Standard+**, dengan konversi antar satuan). Warehouse sengaja tidak masuk sini (nanti di modul Inventory). |
| **Fondasi Inventory/Persediaan** ([03](03-inventory-foundation.md)) | **Tier Standard+.** Warehouse 1 level (tanpa sub-lokasi), multi-warehouse tanpa feature flag, Stock Move + generic reference, Stock Reservation (cegah SO menyalip), Serial/Lot dari awal, 3 metode valuasi (FIFO/Average/Specific, tanpa LIFO sesuai PSAK 14) + sistem pencatatan Perpetual/Periodik — keduanya global, cuma bisa diubah setelah tutup buku. Depends ke `c18_common` & `c18_account`. |
| **Siklus Pembelian** ([04](04-purchase.md)) | **Tier Standard+.** PO (tanpa PR) → Penerimaan Barang (partial, ada popup sisa qty) → Vendor Bill (partial) → Uang Muka → Pembayaran Vendor (multi-invoice + field "Deductions" ala Accurate) → Retur (partial) → Write-off. Workflow PO cuma `draft`→`confirmed`. Depends `c18_common`/`c18_stock`/`c18_account`. |
| **Siklus Penjualan** ([05](05-sales.md)) | **Tier Standard+.** SO (tanpa Quotation terpisah) → Delivery (partial) → Sales Invoice (partial) → Uang Muka → Penerimaan Piutang (multi-invoice + Deductions) → Retur (partial) → Write-off. Simetris penuh dg Purchase. Depends `c18_common`/`c18_stock`/`c18_account`. |
| **Accounting (Business Layer)** ([06](06-accounting-business.md)) | Kas Bank (Kas Masuk/Keluar/Transfer — **tier Basic+**, `draft`→`posted`), Aktiva Tetap otomatis (penyusutan garis lurus, PPh Final revaluasi via field manual) & 4 Laporan Keuangan (Neraca/Laba Rugi/Trial Balance/Buku Besar) — **tier Standard+**. Bank Reconciliation & Budget ditunda ke Enterprise. |
| **Demo Data untuk Presentasi Client** ([08](08-demo-data.md)) | ~13 transaksi contoh (Kas Bank, siklus Pembelian/Penjualan, Aktiva Tetap, Payroll), digenerate manual lewat `odoo shell` (`c18.basic.erp.demo.generator._generate()`, idempotent) — mekanisme demo data bawaan Odoo tidak bisa dipakai karena modul `auto_install`. |
| **Laporan Keuangan** ([09](09-laporan-keuangan.md)) | **Tier Standard+.** Requirement field-level 7 laporan (Trial Balance, Buku Besar, Neraca, Laba Rugi, Buku Bantu Piutang, Buku Bantu Hutang, Laporan Perubahan Ekuitas sesuai PSAK 1), 3 di antaranya juga punya mode "12 Bulan" (Trial Balance/Neraca/Laba Rugi). **SEMUA 9 laporan/mode SUDAH selesai & diverifikasi** (arsitektur AbstractModel+controller+OWL, pola dari referensi `odoo18_toso`). Sisa cuma export PDF/Excel (opsional). |

## Belum Dibahas / Belum Lengkap

| Dokumen | Ringkasan | Catatan |
|---|---|---|
| **HRIS & Payroll** ([07](07-hris.md)) | Modul `c18_hris` (gabungan). Data karyawan + kontrak (riwayat bertanggal) + Attendance/Time Off sederhana + payroll (salary rule fleksibel, PPh 21 TER, BPJS+UMK, Cost Center). **Tier Enterprise, tapi tetap MVP inti.** | Sisa: riset ke 3 repo referensi payroll (`odoo18-toso`, `odoo18-cep`, `odoo10-kp3`) buat detail teknis PPh 21/BPJS/salary rule. |

## Ditunda (Sengaja)
- **Bank Reconciliation** & **Budget** — ditunda ke tier Enterprise, bukan bagian MVP (Basic/Standard). Lihat [06](06-accounting-business.md).

## Status Implementasi Kode
- `c18_basic_erp` (nama teknis lama: `c18_account`) — **sudah discaffold sebagian** di [`app/mvp/c18_basic_erp/`](../../app/mvp/c18_basic_erp/): model fondasi (CoA, Journal, Move/Move Line, Cost Center, Exchange Rate) + Partner extend + Product tier Basic + Kas Bank (Kas Masuk/Keluar/Transfer) + PO/SO ringan + **Penerimaan Barang → Pembelian (Vendor Bill)** dan **Pengiriman Barang → Penjualan (Customer Invoice)** lengkap dengan **engine costing FIFO/Average** (`c18.stock.layer` + `qty_on_hand`/`avg_cost` di `c18.product`, `costing_method` di `res.company`) + data default (41 akun CoA, 30 master Journal + sequence). **Belum smoke test.**
  - Alur yang sudah jalan: PO→Penerimaan Barang (debit Persediaan/kredit Hutang Belum Difaktur, nambah stok) → tombol "Buat Tagihan" → Pembelian (kredit Hutang Usaha, debit GRNI atau bebas). SO→Pengiriman Barang (debit HPP/kredit Persediaan, konsumsi stok FIFO/Average) → Penjualan (debit Piutang/kredit Pendapatan, plus auto-generate pasangan HPP/Persediaan kalau invoice langsung tanpa Pengiriman Barang terpisah).
  - **Baru ditambahkan (2026-08-27)**: Uang Muka Pembelian/Penjualan (`c18.purchase.advance`/`c18.sale.advance`), Pembayaran Vendor/Penerimaan Piutang (`c18.purchase.payment`/`c18.sale.receipt`), Retur Barang Vendor/Customer (`c18.purchase.return`/`c18.sale.return`), Write-off Hutang/Piutang (`c18.purchase.writeoff`/`c18.sale.writeoff`).
  - **Simplifikasi yang diambil (belum dikonfirmasi user, tandai kalau mau diganti)**:
    1. Deduction di Pembayaran Vendor/Penerimaan Piutang diimplementasikan **level header** (1 set deduction utk seluruh dokumen), bukan **per-baris invoice** + wizard popup seperti draft requirement asal — supaya tidak butuh transient wizard model terpisah. Efek jurnal & arah debit/kredit tetap sama persis sesuai spec.
    2. Retur Barang Customer (E.5) butuh estimasi "harga HPP asal" - kalau match ke `delivery_ref_id` jelas dipakai cost asli, tapi kalau tidak match/nggak ada delivery, fallback ke `_last_cost()` (harga layer terbaru/avg_cost saat ini, bukan trace exact ke dokumen sumber) - ada field `unit_cost_hint` buat override manual.
    3. Retur Barang Vendor (D.5) & Write-off pakai field `amount_paid` di Bill/Invoice buat mengurangi `amount_residual` (bukan field terpisah) - konsisten secara akuntansi, cuma penamaan field agak longgar dari makna "sudah dibayar" literalnya.
  - **Baru ditambahkan (2026-08-27, lanjutan)**: Aktiva Tetap Basic (`c18.fixed.asset.entry`, form generik 1 model utk 5 jenis transaksi), Payroll Basic (`c18.payroll.accrual`/`c18.payroll.payment`), Pemakaian Sendiri (`c18.stock.consume`), Stok Opname (`c18.stock.opname`).
  - **Baru ditambahkan (2026-08-27, penutup)**: Tutup Buku lengkap — Penyesuaian Awal Tahun & Penyesuaian Akhir Tahun (reuse `c18.account.move`, tanggal otomatis dipaksa ke 1 Jan/31 Des via override `create()`/`write()`), proses "Proses Tutup Buku" (`c18.account.closing`, nolkan semua akun Pendapatan/Beban tahun berjalan ke Laba Ditahan, tandai `is_closing_entry=True` biar bisa dikecualikan dari Laporan Laba Rugi nanti), plus **penguncian periode** (blokir create/write/unlink jurnal apapun di tanggal ≤ tutup buku terakhir).
  - **Seluruh scope form transaksi tier Basic (dokumen [06-accounting-business.md](06-accounting-business.md) poin A-K, termasuk Tutup Buku) kini sudah ada modelnya — total ±33 model.** Semua 33 xmlid akun/journal yang direferensikan lewat `env.ref()` sudah divalidasi ada (cross-check otomatis, 0 missing).
  - **Laporan Keuangan (7, termasuk mode 12 Bulan) & Kartu Stok/Saldo Persediaan — SEMUA SUDAH SELESAI & diverifikasi (2026-08-30)**, lihat [09-laporan-keuangan.md](09-laporan-keuangan.md) & [06-accounting-business.md poin F](06-accounting-business.md#f-persediaan--mekanisme-costing-fifoaverage-2026-08-26-melengkapi-gap-belum-diputuskan-sebelumnya). Sisanya yang **belum ada sama sekali**: seluruh scope tier Standard+ (Aktiva Tetap otomatis/penyusutan, dst — lihat [00-tiering-produk.md](../00-tiering-produk.md)).
  - **Sudah smoke test (2026-08-27)** ke instance Odoo sungguhan lewat `docker compose` (`app/docker/`), database terpisah `test_c18_basic_erp` (bukan database kerja utama, di-rollback stlh test — tidak ada data nyantol). Install bersih (0 error), data default cocok (41 akun/30 journal/30 sequence). 7 skenario end-to-end via `odoo shell` semua **PASS**: Jurnal Umum (balance check + reject unbalanced), PO→Penerimaan Barang→Buat Tagihan→Pembelian, SO→Pengiriman Barang (FIFO costing benar)→Penjualan (no double-COGS krn ada `delivery_ref_id`), Stok Opname, Tutup Buku (4 baris jurnal ke Laba Ditahan), penguncian periode (jurnal di tanggal tertutup ditolak). Container di-stop stlh test (data volume/pgdata tetap ada).
  - Belum ditest lewat UI browser (form/menu belum diklik manual) — baru functional test via `odoo shell`/ORM langsung. Belum test skenario Average costing (cuma FIFO), deduction Payment/Receipt, Retur, Write-off, Aktiva Tetap, Payroll secara eksplisit (modelnya konsisten pola yang sudah ditest, resiko rendah tapi belum diverifikasi langsung).
  - **Gap multi-currency ditemukan & diperbaiki (2026-08-30)**: agregasi lintas jurnal (Tutup Buku, rekonsiliasi Aktiva Tetap, Laporan Daftar Aktiva Tetap) sebelumnya menjumlah `debit`/`credit`/`amount` mentah tanpa konversi ke company currency — salah kalau ada jurnal non-company-currency tercampur. Diperbaiki: tambah computed stored field `debit_company_currency`/`credit_company_currency` di `c18.account.move.line` (`debit`/`credit` × `exchange_rate` jurnal), semua titik agregasi diarahkan pakai field itu; `c18.fixed.asset.entry` juga ditambah field `exchange_rate` sendiri (sebelumnya tidak ada, jurnalnya selalu default kurs 1.0 walau `currency_id` beda) + field itu di-expose di form. Aturan konversi ini juga dituliskan eksplisit di [01-accounting-foundation.md poin 5](01-accounting-foundation.md#5-multi-currency) supaya tidak ambigu lagi. **Sudah diverifikasi** via `odoo shell` di `test_c18_basic_erp` (skenario cross-currency, company currency vs currency asing beda, hasil konversi & agregasi Tutup Buku benar) — data test di-rollback. Field computed baru ini perlu `-u c18_basic_erp` (module update) saat dideploy ke database yang sudah ada datanya, supaya ter-backfill.
- `c18_stock` — **belum dibuat sama sekali**, tapi requirement (dokumen 03) sudah cukup lengkap, **siap mulai implementasi** (setelah `c18_account` matang).
- `c18_purchase` — **belum dibuat sama sekali**, tapi requirement (dokumen 04) sudah cukup lengkap, **siap mulai implementasi**.
- `c18_sale` — **belum dibuat sama sekali**, tapi requirement (dokumen 05) sudah cukup lengkap, **siap mulai implementasi**.
- `c18_hris` — **belum dibuat sama sekali**, requirement (dokumen 07) masih perlu riset payroll lebih lanjut sebelum siap implementasi penuh.

**Catatan (2026-08-27)**: `c18_common` sebagai modul terpisah **tidak dipakai lagi** — per koreksi di [00-tiering-produk.md](../00-tiering-produk.md), Partner extend & Product versi Basic ternyata bagian dari `c18_account` sendiri (bukan modul shared terpisah), supaya tier Basic cukup 1 modul yang di-install.
