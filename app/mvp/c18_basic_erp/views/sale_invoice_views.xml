<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_c18_sale_invoice_list" model="ir.ui.view">
        <field name="name">c18.sale.invoice.list</field>
        <field name="model">c18.sale.invoice</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="date"/>
                <field name="amount_total"/>
                <field name="amount_residual"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_c18_sale_invoice_form" model="ir.ui.view">
        <field name="name">c18.sale.invoice.form</field>
        <field name="model">c18.sale.invoice</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_post" type="object" string="Post" class="oe_highlight" invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,posted"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="partner_id" readonly="state != 'draft'"/>
                            <field name="date" readonly="state != 'draft'"/>
                            <field name="so_ref_id" readonly="state != 'draft'"/>
                            <field name="delivery_ref_id" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="credit_account_id" readonly="state != 'draft'"/>
                            <field name="cost_center_id" readonly="state != 'draft'"/>
                            <field name="amount_paid" readonly="1"/>
                            <field name="amount_residual" readonly="1"/>
                            <field name="move_id" readonly="1"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <field name="line_ids" readonly="state != 'draft'">
                        <list editable="bottom">
                            <field name="product_id"/>
                            <field name="name"/>
                            <field name="qty"/>
                            <field name="price_unit"/>
                            <field name="subtotal"/>
                        </list>
                    </field>
                    <group class="oe_subtotal_footer">
                        <field name="amount_total"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_c18_sale_invoice" model="ir.actions.act_window">
        <field name="name">Customer Invoice</field>
        <field name="res_model">c18.sale.invoice</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
